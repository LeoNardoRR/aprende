# Manual do administrador

Administre ambientes, papéis, integrações e operação pelo princípio do menor privilégio. Nunca use `service_role` no navegador. Mudanças de banco são migrations aditivas testadas desde banco vazio. Consulte auditoria por escopo e correlation ID, execute restores em ambiente descartável e siga os runbooks para incidente e rollback.
